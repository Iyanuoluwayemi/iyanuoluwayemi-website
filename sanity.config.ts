import { defineConfig } from 'sanity';

export default defineConfig({
  name: 'default',
  title: 'Portfolio Studio',
  projectId: 'z2w2n5ig',
  dataset: 'production',
  basePath: '/studio',
  schema: {
    types: [],
  },
});
